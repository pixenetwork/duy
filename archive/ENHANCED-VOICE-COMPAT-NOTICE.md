# Experimental Voice Compatibility Adapter

The previously created `pixel_admin_voice_enhanced_compat` v0.2.0 package was based on Enhanced's deprecated Mumble compatibility behavior. Pixel Network is now Enhanced-only with no legacy/Mumble/pma dependency, so that adapter is intentionally excluded from active resources.

It remains a separately delivered historical package and must not be enabled in the Pixel Network production configuration.
